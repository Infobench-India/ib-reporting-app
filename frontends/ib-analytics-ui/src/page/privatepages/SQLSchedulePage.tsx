import React from "react";
import SchedulerList from "../../components/Scheduler/SchedulerList";

const SQLSchedulePage: React.FC = () => {
    return (
        <div className="container-fluid">
            <SchedulerList />
        </div>
    );
};

export default SQLSchedulePage;
